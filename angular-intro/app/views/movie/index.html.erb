
  <div ng-controller="IndexCtrl">
    <h1>{{title}}</h1>
    <div ng-repeat="movie in movies | orderBy:'name' | limitTo: 3">
      <p>{{movie.name}}  </p>
    </div>
  </div>

  <input type="text" ng-model="favoriteColor" placeholder="Favorite color">
